{
  "name": "movie-finder",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "types": "wrangler types",
    "db:migrate": "wrangler d1 migrations apply movie-finder-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply movie-finder-db --remote"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.2.0",
    "astro": "^7.2.0"
  }
}
